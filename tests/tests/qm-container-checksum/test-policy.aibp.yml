name: test-policy
description: Test policy to enable boot check

restrictions:
  modes:
    allow:
      - image

  variables:
    force:
      policy_require_qm_digest: true
      policy_require_qm_separate_partition: true
